workflows:
  ios-workflow:
    name: iOS Workflow
    instance_type: mac_mini_m1
    environment:
      xcode: latest
      node: latest
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Add iOS platform
        script: |
          npx cap add ios
          ls -la ios/App/ || echo "iOS folder not created!"
      - name: Sync Capacitor
        script: npx cap sync ios
      - name: Debug
        script: ls -la ios/App/
      - name: Build iOS
        script: |
          xcodebuild -workspace ios/App/App.xcworkspace \
            -scheme App \
            -configuration Release \
            -archivePath ios/App/App.xcarchive \
            archive CODE_SIGNING_ALLOWED=NO
    artifacts:
      - ios/App/App.xcarchive
